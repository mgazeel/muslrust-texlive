[package]
authors = ["clux <sszynrae@gmail.com>"]
name = "hypercrate"
version = "0.1.0"
edition = "2018"

[dependencies]
hyper = { version = "0.14", features=["client","http1"] }
tokio = { version = "1.12.0", features=["macros", "io-std", "io-util"] }
hyper-tls = "0.5.0"
